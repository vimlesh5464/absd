<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <% if (successMsg.length > 0) { %>
    <p style="color: green;"><%= successMsg[0] %></p>
  <% } %>
  
  <% if (errorMsg.length > 0) { %>
    <p style="color: red;"><%= errorMsg[0] %></p>
  <% } %>
  
  <h1>Hello, <%= name %></h1>
  
</body>
</html>